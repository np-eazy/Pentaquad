// A file of other parameters that don't change the game per se but affect the
// performance and detailed computations.

// Collision window radius to save on collision calculations
export const COLLISION_RADIUS = 3;
